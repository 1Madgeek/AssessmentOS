node_modules
dist
.next
.turbo
.env
.env.local
*.log
coverage
.DS_Store
*.tsbuildinfo
apps/web/out
.drizzle
.idea/
.pnpm-store/

# Local uploaded assets (rich-text images)
data/

# Local MCP credentials (do not commit)
.cursor/mcp.json
.cursor/mcp-token
.cursor/.mcp-token

# MCP npm publish staging
apps/mcp/.publish/

# K8s / deploy — site-specific and secrets (never commit)
deploy.env
k8s/02-secret.yaml
k8s/generated/
